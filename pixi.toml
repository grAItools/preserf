[workspace]
channels = ["conda-forge"]
platforms = ["linux-64", "linux-aarch64", "osx-arm64"]

[dependencies]
fortran-compiler = "*"
netcdf-fortran = ">=4.6.0"
python = ">=3.12"

[pypi-dependencies]
preserf = { path = ".", editable = true }

[feature.dev.dependencies]
cmake = ">=3.20"
c-compiler = "*"
cxx-compiler = "*"
dprint = ">=0.47"
netcdf4 = ">=1.7.4"
numpy = ">=1.26"

[feature.dev.pypi-dependencies]
fprettify = ">=0.3"
mypy = ">=1.0"
pytest = ">=8.0"
ruff = ">=0.8"

[environments]
default = { features = [], solve-group = "default" }
dev = { features = ["dev"], solve-group = "default" }

[tasks]
fmt-py = { cmd = ["ruff", "format", "src", "tests"], default-environment = "dev" }
fmt-py-src = { cmd = ["ruff", "format"], default-environment = "dev" }
fmt-f = { cmd = ["fprettify", "--recursive", "src/preserf-fortran"], default-environment = "dev" }
fmt-f-src = { cmd = ["fprettify"], default-environment = "dev" }
fmt = { depends-on = ["fmt-py", "fmt-f", "fmt-d"] }
fmt-py-check = { cmd = ["ruff", "format", "--check", "src", "tests"], default-environment = "dev" }
fmt-f-check = { cmd = ["python", "-c", "import subprocess,sys;r=subprocess.run(['fprettify','--diff','--recursive','src/preserf-fortran'],capture_output=True,text=True);print(r.stdout,end='');sys.exit(1 if r.stdout.strip() or r.returncode != 0 else 0)"], default-environment = "dev" }
fmt-d = { cmd = ["dprint", "fmt"], default-environment = "dev" }
fmt-d-check = { cmd = ["dprint", "check"], default-environment = "dev" }
fmt-check = { depends-on = ["fmt-py-check", "fmt-f-check", "fmt-d-check"] }
lint = { cmd = ["ruff", "check", "src", "tests"], default-environment = "dev" }
test = { cmd = ["pytest"], default-environment = "dev" }
test-all = { cmd = ["pytest"], default-environment = "dev" }
typecheck = { cmd = ["mypy", "src"], default-environment = "dev" }
verify = { depends-on = ["fmt-check", "lint", "typecheck", "test"] }
build-fortran = { cmd = ["sh", "-c", "cmake -S src/preserf-fortran -B build/preserf-fortran && cmake --build build/preserf-fortran"], default-environment = "dev" }
test-fortran = { cmd = ["ctest", "--test-dir", "build/preserf-fortran", "--output-on-failure"], default-environment = "dev" }
clean-fortran = { cmd = ["rm", "-rf", "build/preserf-fortran"], default-environment = "dev" }
# clean = { cmd = ["rm", "-rf", "build", "dist", "preserf.egg-info"] }
