# OpenCode commands

OpenCode slash commands live here. By default this directory is empty; the
cross-tool slash commands are maintained under `.claude/commands/` because
that's where most teams in 2026 author them first. If you use OpenCode
primarily, move them here and reference them from `.claude/commands/` via
symlinks instead.
